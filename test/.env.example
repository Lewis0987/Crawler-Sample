# 複製本檔為 .env 後填入。.env 已被 .gitignore 排除，勿提交。
# 優先序：環境變數/.env > login_config.json > client.login() 傳入值

HMI_USERNAME=hmiUser
# 前端 DevTools 抓到的可重放 password 密文（hex，勿加引號）
LOGIN_PASSWORD_PAYLOAD=<paste replay payload here>