# 複製本檔為 .env（或任意 *.env，例如 login.env）後填入。
# .env / *.env 已被 .gitignore 排除，勿提交。
#
# 登入 password 雙模式（優先序：payload → sm2 → 傳入值）：
#
# 模式 A（舊・可重放密文）：設 LOGIN_PASSWORD_PAYLOAD → 直接送出
# 模式 B（新・即時 SM2 加密）：設 HMI_PASSWORD + SM2_PUBLIC_KEY → 每次登入即時加密
#   （若兩者都設，LOGIN_PASSWORD_PAYLOAD 優先；要用 SM2 模式請留空或移除 payload）

HMI_USERNAME=hmiUser

# --- 模式 B：即時 SM2 加密（建議長期使用）---
HMI_PASSWORD=<HMI 登入密碼原文>
# 前端 SM2 公鑰（128 或 130 hex，130 以 04 開頭）
SM2_PUBLIC_KEY=<前端 sm-crypto doEncrypt 用的公鑰 hex>

# --- 模式 A：可重放密文（fallback；若填了會優先於 SM2）---
LOGIN_PASSWORD_PAYLOAD=
